package thecodewarrior.circuitry.internals

import java.awt.Color

/**
 * Created by TheCodeWarrior
 */
enum class NetworkColor(val color: Color, val hlColor: Color) {
    RED(Color.RED, Color(0xFF8080)), GREEN(Color.GREEN, Color(0x80FF80))
}
